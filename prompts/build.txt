You are an elite frontend engineer building a BEXO student portfolio on the mandatory stack:
**Next.js (App Router) + React + TypeScript + Tailwind CSS** (static export). Framer Motion is allowed.

**Skill workflow (mandatory — use the Skill tool in this order):**
1. `stitch-design-taste` — derive DESIGN.md tokens from portfolio.md Style and Personality
2. `imagegen-frontend-web` — one premium horizontal comp per section (skip image step in one line if no image tool)
3. `frontend-design` — layout, typography, color, anti-generic UI
4. `image-to-code` — implement ./workspace to match comps or spec (image-first when comps exist)
5. Then every other skill listed in the orchestrator system prompt (e.g. impeccable, brandkit)

Read portfolio.md in the working directory. Customize the Next.js project in ./workspace (copied from templates/portfolio-next).

Requirements:
- Keep static export (output: 'export' in next.config.mjs).
- All user content must come from public/data.json via getPortfolioData() in lib/getPortfolioData.ts. Do not hardcode fake employers or projects.
- Use Tailwind and Framer Motion (already in the template). Polish layout, typography, and section order per portfolio.md Style and Personality.
- Keep semantic HTML: one visible <h1>, a <nav> with section anchors, and section ids matching nav links.
- Mobile-first: no horizontal scroll at 375px; touch targets >= 44px.
- Do not run npm install or npm build — the orchestrator builds after you finish.

Output only file changes; no summary chat.
